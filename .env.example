# Server Configuration
# Railway automatically sets PORT - do not override unless testing locally
PORT=3000

# Node environment (development or production)
NODE_ENV=production

# Hostname - use 0.0.0.0 in production for Railway
# Railway requires binding to 0.0.0.0 to accept external connections
HOSTNAME=0.0.0.0

# Client Configuration
# Set this to your Railway public URL after first deployment
# Example: https://your-app.up.railway.app
NEXT_PUBLIC_SOCKET_URL=https://your-app.up.railway.app

# Optional: Custom domain if configured
# NEXT_PUBLIC_SOCKET_URL=https://your-custom-domain.com
